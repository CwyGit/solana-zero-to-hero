# 🎯 Assembly备忘录挑战 - 参考答案

使用**内联汇编**实现极致性能的备忘录！

---

## 📋 为什么用汇编？

| 方式 | CU消耗 | 难度 |
|------|--------|------|
| Anchor | 高 | 低 |
| Pinocchio | 中 | 中 |
| Assembly | 低 | 高 |

---

## 💻 核心代码

```rust
#![feature(asm)]

pub fn memo_asm(message: &[u8]) {
    unsafe {
        // 直接调用 sol_log_ syscall
        core::arch::asm!(
            "call sol_log_",
            in("r10") message.as_ptr(),
            in("r11") message.len(),
        );
    }
}
```

---

## ⚠️ 注意事项

1. 需要 `nightly` Rust
2. 直接操作寄存器
3. 无安全检查

---

**制作人**：bruceCao  
**难度**：⭐⭐⭐⭐（高级）
