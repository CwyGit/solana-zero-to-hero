# 📘 测试工具合集 - 学习笔记

> 包含：Mollusk测试、Litesvm测试、Surfpool测试

---

## 📋 测试工具对比

| 工具 | 类型 | 特点 | 适用场景 |
|------|------|------|----------|
| Mollusk | 单元测试 | 轻量快速 | 程序逻辑验证 |
| Litesvm | 本地模拟 | 完整SVM | 集成测试 |
| Surfpool | 本地节点 | 真实环境 | 端到端测试 |

---

## 🔑 Mollusk测试

**特点**：直接调用程序，无需启动验证器

```rust
use mollusk::Mollusk;

#[test]
fn test_instruction() {
    let mollusk = Mollusk::new(&program_id, program_data);
    let result = mollusk.process_instruction(&ix, &accounts);
    assert!(result.is_ok());
}
```

---

## 🔑 Litesvm测试

**特点**：完整的Solana VM模拟

```rust
use litesvm::LiteSvm;

#[test]
fn test_transaction() {
    let mut svm = LiteSvm::new();
    svm.add_program(program_id, program_data);
    let result = svm.process_transaction(&tx);
    assert!(result.is_ok());
}
```

---

## 🔑 Surfpool测试

**特点**：本地开发节点，支持热重载

```bash
# 启动Surfpool
surfpool start

# 部署程序
surfpool deploy ./target/deploy/program.so
```

---

## 📝 测试策略

1. **单元测试(Mollusk)**：测试单个指令逻辑
2. **集成测试(Litesvm)**：测试多指令交互
3. **端到端测试(Surfpool)**：模拟真实用户操作

---

**学习建议**：从Mollusk开始，逐步使用更完整的测试环境！
