# 📘 Web3.js使用指南 - 学习笔记

> 包含：Web3.js Token2022、Web3.js SPL代币

---

## 📋 @solana/web3.js 核心模块

| 模块 | 用途 |
|------|------|
| `Connection` | 连接RPC节点 |
| `Keypair` | 密钥对管理 |
| `Transaction` | 交易构建 |
| `PublicKey` | 公钥操作 |
| `sendAndConfirmTransaction` | 发送交易 |

---

## 💻 连接和发送交易

```javascript
import { Connection, Keypair, Transaction } from '@solana/web3.js';

// 连接
const connection = new Connection('https://api.devnet.solana.com');

// 创建钱包
const wallet = Keypair.generate();

// 发送交易
const signature = await sendAndConfirmTransaction(
    connection,
    transaction,
    [wallet]
);
```

---

## 💻 SPL Token操作

```javascript
import { 
    createMint, 
    getOrCreateAssociatedTokenAccount,
    mintTo,
    transfer 
} from '@solana/spl-token';

// 创建代币
const mint = await createMint(
    connection, payer, mintAuthority, freezeAuthority, 9
);

// 创建Token账户
const tokenAccount = await getOrCreateAssociatedTokenAccount(
    connection, payer, mint, owner
);

// 铸造代币
await mintTo(connection, payer, mint, tokenAccount.address, mintAuthority, 1000000000);
```

---

## 📝 学习检查清单

- [ ] 会使用Connection连接RPC
- [ ] 能创建和发送Transaction
- [ ] 掌握SPL Token的JavaScript操作

---

**学习建议**：结合前端框架(React/Vue)构建完整DApp！
</Parameter>
<parameter name="Complexity">3
